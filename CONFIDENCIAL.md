# Propriedade intelectual — RCT Plataforma

**Titular:** Tiago Aureliano da Rocha

## Não versionar

- `private/` e todo seu conteúdo (incl. `private/treinamento/*.docx`, `private/scripts/`)
- `scripts/` — scripts operacionais confidenciais (geração de posts, validação de ambiente, etc.)
- `.env`, `.env.local`, `*.bak_*`
- `CURSOR_PROMPT*.md`, `*_PROMPT*.md`
- Materiais `RCT_Fase*_*.docx` e `RCT_*_Confidencial.docx`

Copie `.env.example` e preencha localmente. Os scripts ficam em `private/scripts/` apenas na máquina local.
